<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <p class="izq"></p>
    <a class="navbar-brand" href="#" id="logo">Calendario</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link" href="#">Inicio<span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">tareas pendientes</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Quienes somos</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Contactenos</a>
        </li>
      </ul>
      <% if user_signed_in? %>
        <%= button_tag "Cerrar Sesión", :type => 'button', method: :delete, :class => "btn btn-outline-success my-2 my-sm-0", :onclick => "location.href = '#{destroy_user_session_path}'"%>
      <% else %>
        <%= button_tag "Registrarse", :type => 'button', :class => "btn btn-outline-success my-2 my-sm-0", :onclick => "location.href = '#{new_user_registration_path}'" %>
        <p class="izq"></p>
        <%= button_tag "Iniciar Sesión", :type => 'button', :class => "btn btn-outline-success my-2 my-sm-0", :onclick => "location.href = '#{new_user_session_path}'" %>
        <p class="izq"></p>
      <% end %>
    </div>
  </nav>
</header>