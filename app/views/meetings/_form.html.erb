<div class="mx-auto" style="width: 600px;">
<%= form_with(model: meeting, local: true) do |form| %>
  <% if meeting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meeting.errors.count, "error") %> prohibited this meeting from being saved:</h2>

      <ul>
        <% meeting.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group paddin">
    <%= form.label :name, 'Titulo' %>
    <%= form.text_field :name, :class => "form-control" %>
  </div>


  <div class="form-group paddin">
    <%= form.label :description, 'Descripción' %>
    <%= form.text_area :description, :class => "form-control" %>
  </div>

  <div class="form-group paddin">
    <%= form.label :start_time, 'Inicio de evento' %>
    <%= form.datetime_select :start_time, { ampm: true, order: [:day, :month, :year] }, { class: "form-control1" } %>
  </div>

  <div class="form-group paddin">
    <%= form.label :end_time, 'Fin de evento' %>
    <%= form.datetime_select :end_time, { ampm: true, order: [:day, :month, :year] }, { class: "form-control1" } %>
  </div>

  <div class="actions btnpad2">
    <%= form.submit 'Crear evento',:class => "btn btn-success"%>
  </div>
<% end %>
</div>